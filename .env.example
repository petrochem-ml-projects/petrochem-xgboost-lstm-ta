# Local-dev configuration template — copy to `.env` (gitignored) and adjust if needed.
# Naming convention per CLAUDE.md §12.14: UPPER_SNAKE_CASE, prefixed by owning system.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=petrochem
POSTGRES_USER=petrochem
POSTGRES_PASSWORD=petrochem
MLFLOW_TRACKING_URI=http://localhost:5000
